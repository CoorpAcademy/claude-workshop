# ========================================
# FastAPI Application Configuration
# ========================================
#
# NOTE: This file configures the FastAPI application server.
# It is SEPARATE from the root .env which configures Claude Code workshop features.
#
# REQUIRED FOR: Running the Natural Language MongoDB Query application
# LOADED BY: app/server/server.py via python-dotenv
#
# See README.md "Environment Configuration" section for details.
# ========================================

# MongoDB Configuration
# Connection string for the application database
# Default uses credentials from docker-compose.yml
MONGODB_URI=mongodb://admin:admin123@localhost:27017
MONGODB_DATABASE=nlq_interface

# Anthropic API Key - Required for Claude AI functionality in the FastAPI app
# Get your key at: https://console.anthropic.com/
# NOTE: This is separate from the root .env ANTHROPIC_API_KEY used by Claude Code hooks
ANTHROPIC_API_KEY=your-anthropic-api-key-here