# ========================================
# Claude Code Workshop Infrastructure Configuration
# ========================================
#
# NOTE: This file configures Claude Code workshop features (hooks, MCP integrations).
# It is SEPARATE from app/server/.env which configures the FastAPI application.
#
# REQUIRED FOR: Claude Code hooks, MCP servers, background agents
# OPTIONAL FOR: Running the basic application (see app/server/.env instead)
#
# See README.md "Environment Configuration" section for details.
# ========================================

# ========================================
# Required: Core Workshop Configuration
# ========================================

# Anthropic API Key - Required for Claude Code hooks and MCP integrations
# Get your key at: https://console.anthropic.com/
# NOTE: Also set this in app/server/.env for the application itself
ANTHROPIC_API_KEY=

# MongoDB Connection String - Required for MongoDB MCP server
# Default local MongoDB: mongodb://localhost:27017/workshop
# NOTE: This is for MCP integration, separate from app/server/.env MONGODB_URI
MDB_MCP_CONNECTION_STRING=

# ========================================
# Optional: MCP and Advanced Features
# ========================================

# Firecrawl API Key - Optional (for web scraping MCP integration)
# Get your key at: https://firecrawl.dev/
FIRECRAWL_API_KEY=

# Claude Code Path - Optional (default: 'claude')
# If 'claude' command doesn't work, run 'which claude' and paste the path here
CLAUDE_CODE_PATH=claude

# ========================================
# Optional: Advanced Workshop Features
# ========================================

# GitHub Personal Access Token - Optional (for GitHub integration examples)
# Required only if demonstrating GitHub workflow automation
GITHUB_PAT=

# E2B API Key - Optional (for cloud sandbox agent execution)
# Get your key at: https://e2b.dev/
E2B_API_KEY=

# Maintain Working Directory - Optional (returns to root after each command)
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true
